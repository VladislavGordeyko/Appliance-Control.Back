/* eslint-disable no-unused-vars */
/** Appliance State */
export enum ApplianceStatus {
    WORKING = 'WORKING',
    IDLE = 'IDLE',
    OFFLINE = 'OFFLINE'
}

/** Command State */
export enum CommandStatus {
    INPROCESS = 'INPROCESS',
    FINISHED = 'FINISHED'
}
